#ifndef main_h
#define main_h

#include <stdarg.h>
#include <unistd.h>

int _printf(const char *format, ...);
int _putchar(char c);
int  print_string(char *string);
int  my_puts(char *string);
int put_int(int n);

#endif
